<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:map="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
             x:Class="GpsNotebook.View.AddPinView"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="Save"
            Command="{Binding SavePin}"
            Order="Primary" />
    </ContentPage.ToolbarItems>


    <Grid RowDefinitions="0.55*, *">
        
        <StackLayout Grid.Row="0" Padding="25">

            <Entry Placeholder="Pin name" Text="{Binding PinName}"/>
            <Entry Placeholder="Pin latitude" Text="{Binding PinLatitude}"/>
            <Entry Placeholder="Pin longitude" Text="{Binding PinLongitude}"/>
            <Entry Placeholder="Description" Text="{Binding PinDescription}"/>

        </StackLayout>

        <StackLayout Margin="10" Grid.Row="1">

            <map:Map/>

        </StackLayout>

    </Grid>
    
</ContentPage>