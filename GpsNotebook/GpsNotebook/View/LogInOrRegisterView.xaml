<?xml version="1.0" encoding="utf-8" ?>
<BaseContentPage:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                                 xmlns:BaseContentPage="clr-namespace:GpsNotebook.View"
                                 x:Class="GpsNotebook.View.LogInOrRegisterView"
                                 BackgroundColor="{DynamicResource PageBackgroundColor}">

    <Grid RowDefinitions="0.2*, 0.2*, 0.2*, 0.1*, 0.1*">

        <Image Source="pic_enter_page.png" 
               Grid.Row="1"/>

        <Label Style="{StaticResource label6}"
               Grid.Row="2"
               Text="MapNotepad" 
               HorizontalOptions="Center" />

        <Button Style="{StaticResource button1}"
                Grid.Row="3"
                Text="Log in"
                Margin="15,10,15,10" 
                Command="{Binding LoginCommand}" />

        <Button Style="{StaticResource button2}"
                Grid.Row="4" 
                Text="Create account" 
                Margin="15,10,15,15" 
                Command="{Binding RegisterCommand}"/>
        
    </Grid>
</BaseContentPage:BaseContentPage>